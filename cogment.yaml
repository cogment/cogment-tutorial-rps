
import:
  proto:
    - data.proto

commands:
  build: docker-compose build
  start: docker-compose up orchestrator env agent
  stop: docker-compose stop orchestrator env agent
  play: docker-compose run --rm client

environment:
  config_type: cogment_demo_rps.EnvConfig

trial:
  config_type: cogment_demo_rps.TrialConfig
#  pre_hooks:
#    - grpc://configurator:9000


# Static configuration
actor_classes:
  - id: player
    action:
      space: cogment_demo_rps.Action
    observation:
      space: cogment_demo_rps.Observation


# Dynamic configuration (could be changed by a pre-hook)
trial_params:
  environment:
    endpoint: grpc://env:9000
  actors:
    # - name: human
    #   actor_class: player
    #   endpoint: client
    - name: machine
      actor_class: player
      implementation: random
      endpoint: grpc://agent:9000
    - name: another_machine
      actor_class: player
      implementation: random
      endpoint: grpc://agent:9000
